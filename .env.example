# Copy this file to .env.local and fill in the values from your Supabase project.
# Without these, the app runs fully in "local demo mode" — menu/orders are stored
# in the browser only, and Menu AI uses the free rule-based engine. See SETUP.md.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
